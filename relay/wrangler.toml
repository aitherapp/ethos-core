name = "ethos-private-relay"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
workers_dev = true

# Durable Object binding — wired in Task 4.
[[durable_objects.bindings]]
name = "RELAY"
class_name = "RelayObject"
script_name = "ethos-private-relay"

[observability]
enabled = true
head_sampling_rate = 1

# Optional secrets (CLI — never commit values):
#   RELAY_AUTH_TOKEN
